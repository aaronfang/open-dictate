# Open Dictate 产品路线图

开源的系统级语音输入工具：全局热键 → 录音 → 语音识别 → 任意 App 光标处上屏，并提供 **本地优先、云端可选** 的文本润色。

本文档描述完整产品愿景、当前进度与分阶段目标。实现细节见 [providers.md](providers.md)、[privacy.md](privacy.md)、[building.md](building.md)。

## 产品愿景

- **系统级听写**：不绑定某一编辑器，在任意输入框可用
- **本地优先**：默认音频与识别不出网；云端能力需用户显式开启
- **可插拔**：STT（识别）与 PostProcessor（润色）可替换
- **可复用内核**：Rust Core + UniFFI，供 macOS / 未来其他平台共用
- **个人化**：个人词典、按 App 语气/格式画像

## 现状快照

| 能力 | 状态 | 说明 |
|------|------|------|
| macOS 菜单栏 + 热键听写 | 已完成 | 可配热键；按住 / 点按切换 |
| 录音 → 识别 → 上屏 | 已完成 | HUD + 剪贴板注入；静音门控；无焦点时保留剪贴板 |
| SenseVoice（CoreML） | 已完成 | Swift 侧完整流水线；启动预热、静音裁剪；长音频分段 |
| whisper.cpp | 已完成 | 调用本机 CLI；弱结果时可作对比兜底 |
| Voice Processing | 已完成 | 默认关；可开降噪/AGC；失败回退；停录关闭 VP 以免麦克风指示灯/音量闪避残留 |
| 能量 VAD | 已完成 | 自适应噪声底；首尾裁剪；空录跳过；点按模式尾静音自动停录 |
| 权限引导 | 已完成 | 麦克风 / 辅助功能 |
| 设置窗口 | 已完成 | 引擎 / 录音前端 / 润色 / 词典 / 画像 / Ask / 历史 / 简繁字形 |
| Rust Core / UniFFI | 进行中 | crate 与 Pipeline 已有；macOS 未完整接入打包 |
| 规则润色 / 词典 / App 画像 | 已完成 | Swift MVP 已接听写；纠错可自动进词典；P3 收敛 Core |
| 本地/云端 LLM 润色 | 已完成 | DeepSeek + 本地 Qwen2.5-3B；语义改口 + 偏离回退；简繁统一 |
| Ask AI | 已完成 | 选区 + 语音指令（改写/摘要/翻译等）；只读选区 best-effort（AX / Cmd+C） |
| 本机听写历史 | 已完成 | 默关；保留天数；可清；不存音频 |
| `.app` 打包 | 进行中 | ad-hoc 签名可用；Developer ID / 公证待做 |
| 嘈杂环境增强 | 进行中 | 系统 VP + 能量 VAD 已落地；RNNoise / WebRTC VAD 仍待做 |
| 跨平台客户端 | 远期 | UniFFI 预留 |

## 分阶段路线图

### P0 — 听写体验打磨（近期 / 基本完成）

目标：安静环境下「按住说话 → 文字上屏」稳定可用。

- [x] 全局热键录音与松键识别
- [x] SenseVoice / whisper.cpp 引擎切换
- [x] 文本注入（记住目标 App、Cmd+V）
- [x] SenseVoice 冷启动预热与就绪提示
- [x] 首尾静音裁剪（缓解 `<|nospeech|>` 空结果）
- [x] 设置页分组与文档状态对齐（SenseVoice / 本地 LLM / 可配热键已落地）
- [x] 点按切换听写模式（设置：按住说话 / 点按切换）
- [x] 正式 `.app` 打包与 ad-hoc 签名（`./scripts/package_macos_app.sh`；公证 / Developer ID 仍待做）

### P1 — 嘈杂环境与识别质量

目标：车站、人群等场景下可用率明显提升（无法等同安静环境）。

- [x] 录音前端：系统 Voice Processing（噪声抑制 / AGC）可开关 — 默认关；失败回退；停录关闭 VP 并重建引擎（避免橙色麦克风指示灯 / 系统音量闪避残留）
- [ ] 轻量预处理：高通滤波、强制语言（如中文）选项
- [x] 能量 VAD：自适应噪声底、首尾裁剪、空录跳过、点按模式尾静音自动停录（WebRTC / 更稳 VAD 仍可选）
- [ ] 可选专用降噪（如 RNNoise）再送入 STT
- [ ] 嘈杂场景策略（可选：加强降噪或切换引擎；默认仍优先 SenseVoice）

### P2 — 文本智能

目标：识别结果更像「写出来的字」，并支持个人习惯。

- [x] 规则后处理默认开启（去口癖、空白归一化、去连续重复）；关键词改口仅作 LLM 关闭时的弱兜底 — Swift MVP；P3 收敛 `core/core-llm` rules
- [x] 个人词典管理 UI + 本地 SQLite（schema 对齐 `core-store` `dictionary`；P3 收敛 Core）
- [x] 纠错后自动进词典（词级差分；≥2 字；可关）
- [x] 按 App 画像（语气 / 格式）UI + 本地 `app_profiles`（Swift MVP；P3 收敛 Core）
- [x] 可选云端 LLM 润色（DeepSeek / OpenAI 兼容）；UI 明示出网为「文本」— Swift MVP
- [x] 可选本地 LLM 润色（自动下载 Qwen2.5-3B GGUF + llama-cli，无需 Ollama）
- [x] LLM 语义改口 / 拼写纠正 / 句子重说；偏离检测回退；简繁字形统一（默认简体）
- [x] Ask AI 最小版：选区 + 语音指令 → LLM 改写回填（默认 F6；需开启润色）
- [x] 本机听写历史（默关、可清；见 P4 / privacy）

### P3 — 架构收敛

目标：外壳与内核统一，避免 Swift / Rust 双轨逻辑漂移。

- [ ] macOS 接入 UniFFI 绑定与动态库（见 `scripts/build_macos_core.sh`）
- [ ] 听写主路径走 Core `Pipeline`（STT → 词典 → 润色）
- [ ] 设置、隐私展示与 Core 配置一致
- [ ] `core-audio`：VAD / 重采样 / 环形缓冲（替代部分外壳临时逻辑）

### P4 — 云端与扩展能力

目标：在用户知情前提下提供云端能力与可选历史。

- [ ] 火山 ASR 完整接入（现有 OpenAI 兼容事件流；可选 v3 二进制 transport）
- [ ] 云端 Provider 开关、API Key、当前出网类型（音频 / 文本）明示
- [x] 可选「历史记录」：默关、仅本地、可一键清除（见 [privacy.md](privacy.md)；Swift MVP）

### P5 — 跨平台（远期）

目标：同一 Core，多端外壳。

- [ ] Windows 外壳（热键 + 注入）
- [ ] Android / 其他（Kotlin 等 UniFFI 消费方）
- [ ] 统一配置与词典同步策略（若需要，仍默认本地）

## 功能清单（按模块）

### 听写与交互

| 功能 | 状态 | 备注 |
|------|------|------|
| 菜单栏常驻 | 已完成 | `apps/macos`；语音识别 / 文字润色分列状态 |
| 可配置热键 | 已完成 | 预设 + 录制自定义；默认右 Option；Esc / Command 禁用 |
| 按住说话 / 点按切换模式 | 已完成 | 设置「触发方式」；Ask 共用同一触发方式 |
| HUD 状态反馈 | 已完成 | 录音 / 识别 / 润色 / Ask / 错误 |
| Ask AI（选区语音指令） | 已完成 | 默认 F6；改写/摘要/翻译等；只读选区 AX / Cmd+C |
| 任意 App 上屏 | 已完成 | Accessibility + 剪贴板 |
| 多显示器 / 刘海菜单栏 | 说明 | 图标位置受系统菜单栏拥挤与刘海遮挡影响；非独立多屏 API |

### 音频前端

| 功能 | 状态 | 备注 |
|------|------|------|
| AVAudioEngine 录音 | 已完成 | |
| 16 kHz mono 转换 | 已完成 | `WavConverter` |
| 静音裁剪 / 峰值归一化 | 已完成 | 缓解 nospeech |
| Voice Processing / 降噪 | 已完成 | 默认关；`setVoiceProcessingEnabled`；失败回退；停录释放 I/O。开启时录音中可能压低其他声音（通话闪避） |
| 能量 VAD | 已完成 | `EnergyVAD`：自适应噪声底；裁剪/门控；点按尾静音自动停录 |
| WebRTC / 模型 VAD | 计划中 | P1 可选强化 / P3 `core-audio` |

### STT

| 功能 | 状态 | 备注 |
|------|------|------|
| SenseVoice CoreML | 已完成 | 推荐中文场景 |
| whisper.cpp | 已完成 | CLI |
| 模型下载脚本 | 已完成 | `scripts/download_sensevoice_models.sh` |
| 火山云端 ASR | 计划中 | Core 骨架 |
| 引擎自动降级策略 | 远期 | 用户可选，非默认强绑 Whisper |

### 后处理与个人化

| 功能 | 状态 | 备注 |
|------|------|------|
| 规则去口癖 / 去重复 | 已完成 | 规则层；关键词改口仅 LLM 关闭时兜底 |
| 语义改口 / 拼写纠正 | 已完成 | 开启 LLM 时靠语义理解；偏离回退防摘要删减 |
| 简繁统一 | 已完成 | 设置「汉字字形」；默认简体（CFStringTransform） |
| 个人词典 | 已完成 | 设置页 CRUD + 听写替换；纠错后可自动记入词典（词级） |
| App 画像 | 已完成 | 设置页 CRUD；听写按 Bundle ID 套用语气/格式；tone 写入 LLM prompt |
| DeepSeek 润色 | 已完成 | Swift MVP；默认关；失败回退规则结果 |
| 本地 LLM 润色 | 已完成 | 自动下载 Qwen2.5-3B-Instruct Q4_K_M + llama-server；菜单栏显示进度 |
| Ask AI | 已完成 | 选区 + 语音指令；走同一 LLM provider；可翻译/摘要/改写 |
| 云端 LLM 润色（火山等） | 计划中 | Core 骨架 |

### 存储、隐私与分发

| 功能 | 状态 | 备注 |
|------|------|------|
| 本地 SQLite | 进行中 | Core + Swift 外壳共用 dictionary / history schema；路径可配 |
| 无遥测 | 已完成 | 产品原则 |
| 云端显式启用 | 计划中 | 见隐私模型 |
| 历史记录 | 已完成 | 默关；保留天数；单条/全部清除；不存音频 |
| `.app` 签名分发 | 进行中 | `package_macos_app.sh` ad-hoc 包已可用；公证 / Developer ID 待做 |
| Rust Core 打进 App | 计划中 | P3 |

## Typeless 特性对照

对照对象：[Typeless](https://www.typeless.com/)（官方站点 / [User Guide](https://typeless.cc/en/user-guide.html) / [Key Features](https://www.typeless.com/help/quickstart/key-features) / Pricing，2026）。Typeless 以**云端 AI 听写**为主；Open Dictate 以**本地优先**为产品原则，下列「已实现」指能力对等或可替代，不要求同一实现路径。

### Typeless 完整特性清单

| # | 特性 | Typeless 说明 |
|---|------|----------------|
| T1 | 全局听写 | 任意 App 输入框；快捷键开始/结束；上屏到光标处 |
| T2 | AI 去口癖 | 自动去掉 um / uh / you know 等 |
| T3 | AI 去重复 | 去掉口头重复、赘余 |
| T4 | 改口识别 | 识别「说错再改」并只保留最终意图 |
| T5 | 意图理解 / 措辞优化 | 不只转写，按语义整理成更通顺的书面语 |
| T6 | 自动排版 | 口语列表、步骤、要点 → 结构化文本 |
| T7 | 按 App 语气 | 邮件正式、聊天随意等，随前台 App 适配 |
| T8 | 个人词典 | 手动添加专有名词；亦可自动学习词汇 |
| T9 | 写作风格学习 | 随使用逐渐贴近个人语气与习惯（含个性化进度） |
| T10 | 100+ 语言 | 自动语种检测；可中英混说 |
| T11 | Speak-and-Translate | 用母语说，输出目标语言；独立开关与快捷键 |
| T12 | Ask AI | 选中文本 + 语音指令：改写 / 摘要 / 解释 / 翻译 / 头脑风暴等 |
| T13 | Ask 只读文本 | 网页/文档只读选区也可摘要、解释、翻译 |
| T14 | Ask 联网助手 | 查天气、检索、打开页面并带上上下文（偏 Agent） |
| T15 | 可配置快捷键 | 听写 / Ask / Translate 均可自定义 |
| T16 | 点按切换听写 | 再按一次结束（非仅按住） |
| T17 | 语音校准 | 首次约 30 秒朗读，适配口音 |
| T18 | Whisper mode | Pricing 列出的 Pro 能力（低声/私密听写类） |
| T19 | 本机历史 | 听写历史存本机；可控制保留时长 |
| T20 | 账号与跨端同步 | 登录；词典与偏好跨设备同步（声纹内容不同步） |
| T21 | 跨平台 | macOS / Windows / iOS / Android / Web |
| T22 | 移动端语音键盘 | iOS / Android 系统级键盘形态 |
| T23 | 隐私主张 | 零云端留存、不用于训练、历史仅本机（云端处理音频） |
| T24 | 配额与 Pro | 免费周字数上限、增强精度、高峰优先、团队管理等 |

### 与 Open Dictate 对照

| Typeless | Open Dictate | 差距说明 |
|----------|--------------|----------|
| T1 全局听写 | **已有** | 菜单栏 + 可配热键 + 剪贴板注入 |
| T2 去口癖 | **部分** | 规则后处理 + 可选 LLM；非云端实时「智能编辑」同级 |
| T3 去重复 | **部分** | 规则折叠连续重复 + LLM；复杂语义重复仍靠 LLM |
| T4 改口识别 | **部分→较强** | 开启 LLM 时语义改口（含拼写纠正/句子重说）+ 偏离回退；关 LLM 时关键词弱兜底 |
| T5 措辞优化 | **部分** | 本地/DeepSeek 润色可做；默认关，质量依赖模型 |
| T6 自动排版 | **部分** | App 画像 format + LLM 列表/步骤指令 |
| T7 按 App 语气 | **已有** | `app_profiles` Bundle ID → tone/format |
| T8 个人词典 | **已有** | 手动 CRUD + 听写替换；纠错后自动记入词典（可关） |
| T9 风格学习 | **缺** | 无长期风格画像 / 个性化进度 |
| T10 多语言 | **部分** | SenseVoice/Whisper 有限语种；无「100+ 自动检测+混说」产品体验 |
| T11 边说边译 | **部分（经 Ask）** | 无独立 Translate 模式/热键；可用 F6 Ask「翻译成英文」等指令处理选区。听写时「边说边出译文」未做 |
| T12 Ask AI | **已有** | 选区 + 语音指令（改写/摘要/翻译/解释等，取决于指令与模型） |
| T13 Ask 只读选区 | **已有（best-effort）** | AX 选区失败时回退 Cmd+C；Safari/多数网页可选中文本时可用。部分 App/PDF 仍可能取不到选区 |
| T14 Ask 联网助手 | **缺** | 无检索/打开页面等 Agent |
| T15 可配快捷键 | **部分** | 听写 + Ask 热键可配；独立 Translate 热键未做 |
| T16 点按切换 | **已有** | 设置「按住说话 / 点按切换」 |
| T17 语音校准 | **缺** | 无首次校准流程 |
| T18 Whisper mode | **缺** | 未做 |
| T19 本机历史 | **已有** | 默关；本地 SQLite；保留天数 + 可清 |
| T20 账号同步 | **非目标倾向** | 本地优先；若做应可选且默关 |
| T21–T22 跨平台 / 键盘 | **远期** | P5；移动键盘未规划为近期 |
| T23 隐私模型 | **路径不同** | 我们默认本地 STT；云端需显式开。Typeless 为云端处理 + 宣称零留存 |
| T24 配额 / Pro | **不适用** | 开源本地工具，无 SaaS 配额 |

### 主要未实现特性（按优先级建议）

相对 Typeless / 日常可用性，当前仍缺、且符合「本地优先听写」定位的能力：

1. **Developer ID + 公证（P0 收尾）** — `.app` ad-hoc 包已可用；对外分发需正式签名  
2. **RNNoise / WebRTC VAD（P1 剩余）** — 能量 VAD + 系统 VP 已可用；可再加强端侧降噪与嘈杂场景  
3. **Speak-and-Translate 独立模式（T11 完整版）** — 听写热键直接出译文（与 Ask「翻译选区」不同）  
4. **风格学习（T9）** — 长期语气画像（词典自动学习已完成）  
5. **语音校准（T17）** — 口音适配（本地模型侧可选）  
6. **Ask 取选区兜底增强** — 个别 App/PDF 仍失败时的可见提示与备用路径（T13 主路径已可用）  

可刻意不做或远期再议（与开源本地定位冲突或过重）：

- 账号云同步、SaaS 配额、团队 Pro（T20/T24）  
- 联网 Ask Agent（T14）— 若做须显式出网与权限  
- 移动语音键盘 / 全平台对等（T21–T22）— 见 P5  
- 以云端 ASR 为默认路径（与 Typeless 同构）  
- 把「改口」做成专用模型 — 当前语义 LLM + 偏离回退已覆盖主路径  
- 仅为翻译再做一个与 Ask 重复的选区流程 — Ask 已覆盖「翻译这段」

## 非目标

- 不做默认遥测或匿名使用统计上传
- 不默认上传音频或转写到云端
- 不以「必须联网」为产品前提
- 不追求替代专业远场会议转写（嘈杂多人场景仅尽力提升）
- 不以 Typeless 式账号 SaaS / 云端默认识别为产品前提（对照见上节）

## 相关文档

- [隐私模型](privacy.md)
- [Provider 说明与接入](providers.md)
- [开发/构建说明](building.md)
